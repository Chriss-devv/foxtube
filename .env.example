# Copy to .env and fill in. NEVER commit .env.
# Shared secret between Invidious and the Companion service. Must match
# invidious_companion_key in config/config.yml.
SERVER_SECRET_KEY=CHANGE_ME_COMPANION_KEY

# Host port the Nginx overlay listens on (maps to container :80).
PROXY_PORT=3002
